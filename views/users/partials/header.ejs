<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">


    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">

  
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white py-3">
        <div class="container">
            <a class="navbar-brand" href="/users/home">Sneakify</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="/users/home">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/users/shop">Shop</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/users/about">About</a>
                    </li>
                </ul>
                <% if(locals.shopPage){ %>
                <div class="d-flex">
                        <input type="text" id="search-input"  class="form-control rounded" name="search" placeholder="Search for products..." value="<%= locals.search %>" aria-label="Search"/>
                        <button type="submit">Search</button>
                    </div>
                <% }%>
                <div class="d-flex ms-3">
                    <a href="/users/wishlist" class="btn btn-outline-dark me-2">
                        <i class="fas fa-heart"></i>
                    </a>
                    <a href="/users/cart" class="btn btn-outline-dark me-2">
                        <i class="fas fa-shopping-cart"></i>
                    </a>
                    <a href="/users/profile" class="btn btn-outline-dark">
                        <i class="fas fa-user"></i>
                    </a>
                    <% if (isAuthenticate) { %>
                        <a href="/users/logout" class="btn btn-outline-dark">Logout</a>
                      <% } else { %>
                        <a href="/users/login" class="btn btn-outline-dark">Login</a>
                        <a href="/users/signup" class="btn btn-outline-dark">Signup</a>
                      <% } %>


                </div>
            </div>
        </div>
    </nav>
